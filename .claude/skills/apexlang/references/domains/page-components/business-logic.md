# Business Logic Alias

This compatibility alias exists for older packaged references.

Use `references/domains/README.md` as the canonical domain workflow entrypoint and load the Business Logic section.
