-- =============================================================================
-- Батч 001: Будинки — seed data
-- У кластері немає перечислень і предопределённых елементів (Predefined.xml
-- відсутній у RSD_Дома, RSD_Секции, Организации) — сіяти нічого.
-- Файл залишено як обовʼязковий артефакт конвеєра; наповниться, коли батч
-- з EXTERNAL-цілями (Enum.ЮрФизЛицо тощо) заведе свої lookup-таблиці.
-- =============================================================================
SELECT 'batch 001: no seed data required' AS info FROM dual;
